{
  "name": "mern-dashboard",
  "version": "1.0.0",
  "description": "This is my first attempt at a full-stack web app. My app uses React for frontend, Express.js for routing and middleware, Node for backend, and SQL for the database.",
  "main": "index.js",
  "dependencies": {
    "client": "file:./client",
    "server": "file:./server"
  },
  "scripts": {
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "install": "npm run install:client && npm run install:server",
    "start:client": "cd client && npm start",
    "start:server": "cd server && npm start",
    "start": "npm-run-all --parallel start:client start:server"
  },
  "keywords": [],
  "author": "ajkeast",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
